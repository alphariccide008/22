{
  "name": "sabiocast",
  "version": "2.0.0",
  "private": true,
  "description": "SabioCast marketing site — static HTML + Tailwind (CDN) + vanilla JS",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types sitegen/build.mjs",
    "serve": "node sitegen/serve.mjs",
    "dev": "npm run build && npm run serve"
  }
}
