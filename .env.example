# Telegram lead notifications — set these in Vercel → Project → Settings → Environment Variables
# (and locally in .env.local, which is gitignored)
TELEGRAM_BOT_TOKEN=123456789:your-bot-token
TELEGRAM_CHAT_ID=000000000
